import { Tables } from "@/types";
import React from "react";

function SnippetsList({ snippets }: { snippets: Tables<"snippets">[] }) {
  // return smippets.map((snippet) => {});
}

export default SnippetsList;
